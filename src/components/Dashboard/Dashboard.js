const Dashboard = () => {
    return (
        <section id='dashboard-page' className='dashboard'>
            <h1>Dashboard</h1>
            <section>
            </section>
        </section>
    );
};

export default Dashboard;
